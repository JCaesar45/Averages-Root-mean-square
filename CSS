/* styles.css */

@import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

body {
  margin: 0;
  padding: 0;
  font-family: 'Press Start 2P', cursive;
  background-color: #0d0d0d; /* Classic dark SNES background */
  color: #00ffcc; /* Bright cyan text reminiscent of SNES UI */
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}

.container {
  background-color: #222; /* Slightly lighter for contrast */
  border: 4px solid #ff00cc; /* SNES vibrant border color */
  border-radius: 10px;
  padding: 2rem;
  width: 90%;
  max-width: 600px;
  box-shadow: 0 0 20px #ff00cc, inset 0 0 10px #00ffcc;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* Header styles */
header {
  margin-bottom: 1rem;
}

.title {
  font-family: 'Press Start 2P', cursive;
  font-size: 1.5rem;
  color: #ffcc00; /* SNES yellow-orange */
  text-shadow: 0 0 5px #fff, 0 0 10px #ffcc00;
}

/* Main content styles */
main {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.input-section {
  width: 100%;
  margin-bottom: 1rem;
  display: flex;
  flex-direction: column;
  align-items: stretch;
}

label {
  margin-bottom: 0.5rem;
  font-size: 0.8rem;
  color: #fff;
  text-align: center;
}

#numbersInput {
  font-family: 'Press Start 2P', cursive;
  font-size: 0.8rem;
  padding: 0.5rem;
  border: 3px solid #ff00cc;
  border-radius: 4px;
  background-color: #333;
  color: #00ffcc;
  outline: none;
  transition: border-color 0.3s;
}

#numbersInput:focus {
  border-color: #00ffff;
}

#calculateBtn {
  margin-top: 1rem;
  padding: 0.75rem 1.5rem;
  font-family: 'Press Start 2P', cursive;
  font-size: 0.8rem;
  background-color: #ff00cc;
  color: #fff;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  box-shadow: 0 0 10px #ff00cc;
  transition: background-color 0.3s, box-shadow 0.3s;
}

#calculateBtn:hover {
  background-color: #ff33cc;
  box-shadow: 0 0 15px #ff33cc;
}

/* Result styles */
.result-section {
  margin-top: 2rem;
  width: 100%;
  text-align: center;
}

#resultDisplay {
  font-family: 'Press Start 2P', cursive;
  font-size: 0.8rem;
  background-color: #111;
  padding: 1rem;
  border: 3px solid #00ffff;
  border-radius: 4px;
  min-height: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  word-wrap: break-word;
}

/* Footer styles */
footer {
  margin-top: 2rem;
  font-size: 0.6rem;
  color: #888;
}
